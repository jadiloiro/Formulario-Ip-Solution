# Usado pelo docker-compose.yml para configurar o Postgres e a app na VM.
# Copie para .env (mesma pasta do docker-compose.yml) e ajuste antes de subir.
DB_USERNAME=postgres
DB_PASSWORD=troque-esta-senha
DB_DATABASE=ipsolution
